console.log("Executing the Task 2 ::) ");

let inp = String(prompt("Enter the OS name and the Version number ::) "));
let OS_name = inp.split(' ')[0];
let Version_num = inp.split(' ')[1];
console.log(`The OS name is ${OS_name} and Version is ${Version_num}`);